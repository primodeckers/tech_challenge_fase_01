# Runtime mínimo para a API no container (inferência sklearn).
# O pyproject completo inclui PyTorch/MLflow para notebooks — não são necessários aqui.
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
joblib>=1.3
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0
